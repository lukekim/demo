name: uniswap_swaps_eth_usdc
migrations:
  - name: create_table
    path: migrations/create_table.sql
  - name: create_view
    sql: |
      CREATE VIEW demo_view AS (
        SELECT * FROM demo
      )
