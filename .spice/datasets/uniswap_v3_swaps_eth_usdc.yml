name: uniswap_swaps_eth_usdc
migrations:
  - name: create_table
    sql: |
      CREATE TABLE IF NOT EXISTS demo (
        name VARCHAR,
        id INT
      )
  - name: create_view
    sql: |
      CREATE VIEW demo_view AS (
        SELECT * FROM demo
      )
