name: tim_uniswap_swaps_eth_usdc
migrations:
  - name: create_table
    sql: |
      CREATE TABLE IF NOT EXISTS tim_demo (
        ts INT,
        y FLOAT
      )
  - name: create_view
    sql: |
      CREATE VIEW tim_demo_view AS (
        SELECT * FROM tim_demo
      )
